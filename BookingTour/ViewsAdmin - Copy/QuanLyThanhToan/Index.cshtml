@{
    ViewData["Title"] = "IndexAdmin.html";
    Layout = "_LayoutAdmin";
}
<div class="container-fluid">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="header">
                    <h2>
                        QUẢN LÝ THANH TOÁN
                        <small>Quản lý thông tin giao dịch thanh toán</small>
                    </h2>
                    <ul class="header-dropdown m-r--5">
                        <li class="dropdown">
                            <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="material-icons">more_vert</i>
                            </a>
                            <ul class="dropdown-menu pull-right">
                                <li><a href="/Payment/Create" class="waves-effect waves-block">Thêm Thanh Toán</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="body">
                    <table class="table table-bordered table-striped table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Người Thanh Toán</th>
                                <th>Số Tiền</th>
                                <th>Ngày Thanh Toán</th>
                                <th>Hành Động</th>
                                <th>Phương thức thanh toán </th>
                            </tr>
                        </thead>
                        <tbody>
                            @* @foreach (var payment in Model)
                            {
                                <tr>
                                    <td>@payment.Id</td>
                                    <td>@payment.PayerName</td>
                                    <td>@payment.Amount.ToString("C")</td>
                                    <td>@payment.PaymentDate.ToString("dd/MM/yyyy HH:mm")</td>
                                    <td>
                                        <a href="/Payment/Details/@payment.Id" class="btn btn-info">Xem</a>
                                        <a href="/Payment/Edit/@payment.Id" class="btn btn-warning">Sửa</a>
                                        <a href="/Payment/Delete/@payment.Id" class="btn btn-danger">Xóa</a>
                                    </td>
                                </tr>
                            } *@
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>