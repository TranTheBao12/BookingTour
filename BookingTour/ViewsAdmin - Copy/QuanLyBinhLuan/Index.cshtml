@{
    ViewData["Title"] = "IndexAdmin.html";
    Layout = "_LayoutAdmin";
}
<div class="container-fluid">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="header">
                    <h2>
                        QUẢN LÝ BÌNH LUẬN
                        <small>Quản lý thông tin bình luận</small>
                    </h2>
                    <ul class="header-dropdown m-r--5">
                        <li class="dropdown">
                            <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="material-icons">more_vert</i>
                            </a>
                            <ul class="dropdown-menu pull-right">
                                <li><a href="/Comment/Create" class="waves-effect waves-block">Thêm Bình Luận</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="body">
                    <table class="table table-bordered table-striped table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Tên Người Dùng</th>
                                <th>Nội Dung</th>
                                <th>Ngày Tạo</th>
                                <th>Hành Động</th>
                            </tr>
                        </thead>
                        <tbody>
                           @*  @foreach (var comment in Model)
                            {
                                <tr>
                                    <td>@comment.Id</td>
                                    <td>@comment.UserName</td>
                                    <td>@comment.Content</td>
                                    <td>@comment.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>
                                    <td>
                                        <a href="/Comment/Edit/@comment.Id" class="btn btn-warning">Sửa</a>
                                        <a href="/Comment/Delete/@comment.Id" class="btn btn-danger">Xóa</a>
                                    </td>
                                </tr>
                            } *@
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>